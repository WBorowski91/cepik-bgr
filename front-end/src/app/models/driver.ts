export interface Driver {
    id: number,
    firstName: string,
    lastName: string,
    pesel: string,
    street: string,
    postalCode: string,
    city: string,
    drivingLicenseCategory: string
}